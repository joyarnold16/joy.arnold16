# Copy to keystore.properties (git-ignored) at the repo root for local release builds.
# Generate the keystore first, e.g.:
#   keytool -genkeypair -v -keystore app/release.keystore -alias blastgrid \
#     -keyalg RSA -keysize 2048 -validity 10000
storePassword=
keyAlias=blastgrid
keyPassword=
